export interface Customer{
    id?: number,
    name:string,
    nip?: string,
    city?: string, 
    street?: string, 
    isActive?: boolean,
    isVisible?: boolean,
    sum?: number,
    total?: number,
    phone?: string,
    email?: string
}